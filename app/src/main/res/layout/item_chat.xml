<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="chat"
            type="com.chatychaty.domain.model.Chat" />

        <variable
            name="viewModel"
            type="com.chatychaty.app.SharedViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/ripple_chat_list_item">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/spacing_normal"
            android:background="@color/colorSurface"
            android:contentDescription="@string/user_image"
            app:imageUrl="@{chat.user.imgUrl}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:shapeAppearanceOverlay="@style/ImageViewShapeAppearance"
            tools:src="@drawable/ic_person_24dp" />

        <TextView
            android:id="@+id/name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_normal"
            android:layout_marginTop="@dimen/spacing_extra_small"
            android:layout_marginEnd="@dimen/spacing_normal"
            android:layout_marginBottom="1dp"
            android:fontFamily="@font/roboto_medium"
            android:maxLength="32"
            android:singleLine="true"
            android:text="@{chat.user.name}"
            android:textColor="@color/colorSecondary"
            android:textSize="@dimen/list_chat_item_name_dimen"
            app:layout_constraintBottom_toTopOf="@id/body"
            app:layout_constraintEnd_toStartOf="@+id/iv_order"
            app:layout_constraintStart_toEndOf="@+id/img"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Ali" />

        <TextView
            android:id="@+id/body"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_normal"
            android:layout_marginTop="1dp"
            android:layout_marginEnd="@dimen/spacing_normal"
            android:layout_marginBottom="@dimen/spacing_extra_small"
            android:fontFamily="@font/roboto"
            android:maxLength="32"
            android:singleLine="true"
            android:text="@{viewModel.getLastMessage(chat.chatId)}"
            android:textColor="@color/colorOnSecondary"
            android:textSize="@dimen/list_chat_item_body_dimen"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/iv_order"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toEndOf="@+id/img"
            app:layout_constraintTop_toBottomOf="@+id/name"
            tools:text="Hello " />

        <ImageView
            android:id="@+id/iv_order"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/spacing_small"
            android:contentDescription="@string/order"
            android:padding="@dimen/spacing_small"
            android:src="@drawable/ic_drag_indicator_black_24dp"
            android:visibility="invisible"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
